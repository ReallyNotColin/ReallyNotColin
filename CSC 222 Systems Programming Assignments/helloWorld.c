

/* Preprocessor command; tells a C compiler to include stdio.h file */
#include <stdio.h>

/* Main function; Executable */
int main(void){
	printf("Hello World\n");
	/* Terminates main() function and returns value 0 */
	return 0;
	/* Now go into the terminal and type "./a.out" to output this. */
}
